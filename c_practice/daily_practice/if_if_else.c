#include<stdio.h>

int main()
{
        int a,b;

        printf("enter the two input value\n");
        scanf("%d%d",&a,&b);

        if(a > b)
        {
                printf("print a is grater than b\n");
        }
        else
        {
                printf("print a is less than b\n");
        }
        return 0;
} 
